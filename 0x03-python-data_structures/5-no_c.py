#!/usr/bin/env python3
def no_c(my_string):
    d = len(my_string)
    for i in range(0, d)
        if my_string[i] == C or my_string == C:
            del(my_string[i])
            return (my_string)
